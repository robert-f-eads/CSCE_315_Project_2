rewardsMembers : id, firstName, lastName, phone, email, birthday, rewardPoints : SERIAL, VARCHAR(25), VARCHAR(25), VARCHAR(15), VARCHAR(60), DATE, INT : PRIMARY KEY, NOT NULL, NOT NULL, UNIQUE, NOT NULL UNIQUE, NULL, NOT NULL DEFAULT 0 CHECK (rewardPoints>=0)
employees : id, firstName, lastName, phone, email, isAdmin, hourlyRate, hoursPerWeek, hireDate, endDate, active : SERIAL, VARCHAR(25), VARCHAR(25), VARCHAR(15), VARCHAR(60), BOOLEAN, DECIMAL, DECIMAL, DATE, DATE, BOOLEAN : PRIMARY KEY, NOT NULL, NOT NULL, NOT NULL UNIQUE, NOT NULL UNIQUE, NOT NULL DEFAULT FALSE, NOT NULL CHECK (hourlyRate>=0), NOT NULL CHECK (hoursPerWeek>0), NOT NULL, NULL, NOT NULL DEFAULT TRUE
orderTickets : id, timestamp, customerFirstName, rewardsMemberId, employeeId, orderPriceTotal : SERIAL, TIMESTAMP, VARCHAR(25), INT, INT, DECIMAL : PRIMARY KEY, NOT NULL, NOT NULL, REFERENCES rewardsmembers, REFERENCES employees NOT NULL, NOT NULL  
orderItems : id, orderId, itemNumberInOrder, itemName, itemAmount, itemSize : SERIAL, INT, INT, VARCHAR(60), INT, INT : PRIMARY KEY, REFERENCES ordertickets NOT NULL, NOT NULL, NOT NULL, NOT NULL DEFAULT 1, NOT NULL 
ingredients : id, name, expirationDate, quantityRemaining, measurementUnits, pricePerUnitLastOrder, lastOrderDate, unitsInLastOrder : SERIAL, VARCHAR(60), DATE, DECIMAL, VARCHAR(25), DECIMAL, DATE, DECIMAL : PRIMARY KEY, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL CHECK (pricePerUnitLastOrder>=0), NOT NULL, NOT NULL
orderItemAdditions : id, orderId, itemNumberInOrder, ingredientId : SERIAL, INT, INT, INT : PRIMARY KEY, REFERENCES ordertickets NOT NULL, NOT NULL, REFERENCES ingredients NOT NULL
orderItemSubtractions : id, orderId, itemNumberInOrder, ingredientId : SERIAL, INT, INT, INT : PRIMARY KEY, REFERENCES ordertickets NOT NULL, NOT NULL, REFERENCES ingredients NOT NULL
materials : id, name, size, quantityRemaining, measurementsUnits, itemsPerUnit, pricePerUnitLastOrder, lastOrderDate, unitsInLastOrder : SERIAL, VARCHAR(30), VARCHAR(30), INT, VARCHAR(25), INT, DECIMAL, DATE, DECIMAL : PRIMARY KEY, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL CHECK (pricePerUnitLastOrder>=0), NOT NULL, NOT NULL
products : id, name, price : SERIAL, VARCHAR(60), DECIMAL : PRIMARY KEY, NOT NULL, NOT NULL CHECK (price>=0)
productsToIngredients : productId, ingredientId : INT, INT : REFERENCES products, REFERENCES ingredients