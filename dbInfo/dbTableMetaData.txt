rewardsMembers : id, firstName, lastName, phone, email, rewardPoints : SERIAL, VARCHAR(25), VARCHAR(25), VARCHAR(15), VARCHAR(60), INT : PRIMARY KEY, NOT NULL, NOT NULL, UNIQUE, NOT NULL UNIQUE, NOT NULL DEFAULT 0 CHECK (rewardPoints>=0)
employees : id, firstName, lastName, phone, email, isAdmin, hourlyRate, hoursPerWeek, hireDate, endDate, active : SERIAL, VARCHAR(25), VARCHAR(25), VARCHAR(15), VARCHAR(60), BOOLEAN, DOUBLE, INT, DATE, DATE, BOOLEAN : PRIMARY KEY, NOT NULL, NOT NULL, NOT NULL UNIQUE, NOT NULL UNIQUE, NOT NULL DEFAULT 0, NOT NULL CHECK (hourlyRate>=0), NOT NULL CHECK (hoursPerWeek>0), NOT NULL, NULL, NOT NULL DEFAULT 1
orderTickets : id, timestamp, customerFirstName, rewardsMemberId, employeeId : SERIAL, TIMESTAMP, VARCHAR(25), INT, INT : PRIMARY KEY, NOT NULL, NOT NULL, FOREIGN KEY, FOREIGN KEY NOT NULL  
orderItems : id, orderId, itemNumberInOrder, itemName, itemAmount, itemSize : SERIAL, INT, INT, VARCHAR(30), INT, INT : PRIMARY KEY, FOREIGN KEY NOT NULL, NOT NULL, NOT NULL, NOT NULL DEFAULT 1, NOT NULL 
orderItemAdditions : id, orderId, itemNumberInOrder, ingredientId : SERIAL, INT, INT, INT : PRIMARY KEY, FOREIGN KEY NOT NULL, NOT NULL, FOREIGN KEY NOT NULL
orderItemSubtractions : id, orderId, itemNumberInOrder, ingredientId : SERIAL, INT, INT, INT : PRIMARY KEY, FOREIGN KEY NOT NULL, NOT NULL, FOREIGN KEY NOT NULL
materials : id, name, size, quantityRemaining, measurementsUnits, itemsPerUnit, pricePerUnitLastOrder, lastOrderDate, unitsInLastOrder : SERIAL, VARCHAR(30), VARCHAR(30), INT, VARCHAR(25), INT, DOUBLE, DATE, DOUBLE : PRIMARY KEY, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL CHECK (pricePerUnitLastOrder>=0), NOT NULL, NOT NULL
ingredients : id, name, expirationDate, quantityRemaining, measurementUnits, pricePerUnitLastOrder, lastOrderDate, unitsInLastOrder : SERIAL, VARCHAR(30), DATE, DOUBLE, VARCHAR(25), DOUBLE, DATE, DOUBLE : SERIAL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL CHECK (pricePerUnitLastOrder>=0), NOT NULL, NOT NULL
products : id, name, price : SERIAL, VARCHAR(30), DOUBLE : SERIAL, NOT NULL, NOT NULL CHECK (price>=0)
productsToIngredients : productId, ingredientId : INT, INT : FOREIGN KEY, FOREIGN KEY