rewardsMembers : id, firstName, lastName, phone, email, rewardPoints : SERIAL, VARCHAR(25), VARCHAR(25), VARCHAR(15), VARCHAR(60), INT : PRIMARY KEY, NOT NULL, NOT NULL, UNIQUE, NOT NULL UNIQUE, NOT NULL DEFAULT 0 CHECK (rewardPoints>=0)
employees: id, firstName, lastName, phone, email, isAdmin, hourlyRate, hoursPerWeek, hireDate, endDate, active: SERIAL, VARCHAR(25), VARCHAR(25), VARCHAR(15), VARCHAR(60), BOOLEAN, FLOAT, INT, DATE, DATE, BOOLEAN: PRIMARY KEY, NOT NULL, NOT NULL, UNIQUE, NOT NULL UNIQUE, NOT NULL DEFAULT 0, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL
orderTickets: orderId, timestamp, customerFirstName, rewardsMemberId, employeeId : SERIAL, TIMESTAMP, VARCHAR(25), INT, INT : PRIMARY KEY, NOT NULL, NOT NULL, FOREIGN KEY, FOREIGN KEY  
orderItems: id, orderId, itemNumberInOrder, itemName, itemAmount, itemSize : SERIAL, INT, INT, VARCHAR(25), INT, INT : PRIMARY KEY, FOREIGN KEY, NOT NULL, NOT NULL, NOT NULL DEFAULT 1, NOT NULL 
orderItemAdditions : id, orderId, itemNumberInOrder, ingredientId : SERIAL, INT, INT, INT : NOT NULL UNIQUE, NOT NULL, NOT NULL, NOT NULL
orderItemSubtractions : id, orderId, itemNumberInOrder, ingredientId : SERIAL, INT, INT, INT : NOT NULL UNIQUE, NOT NULL, NOT NULL, NOT NULL
materials : id, name, size, quantityRemaining, measurementsUnits, itemsPerUnit, pricePerUnitLastOrder, lastOrderDate, unitsInLastOrder : PRIMARY KEY, VARCHAR(25), VARCHAR(25), INT, VARCHAR(25), INT, DOUBLE, DATE, DOUBLE : NOT NULL UNIQUE, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL
ingredients : id, name, expirationDate, quantityRemaining, measurementUnits, pricePerUnitLastOrder, lastOrderDate, unitsInLastOrder : SERIAL, VARCHAR(25), DATE, DOUBLE, VARCHAR(25), DOUBLE, DATE, DOUBLE : NOT NULL UNIQUE, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL, NOT NULL
products : id, name, price : SERIAL, VARCHAR(25), DOUBLE : NOT NULL UNIQUE, NOT NULL, NOT NULL
productsToIngredients: productId, ingredientId : INT, INT : FOREIGN KEY, FOREIGN KEY




